#include<stdio.h>
enum days{
    Monday,Tuesday,Wednesday,Thursday,Friday,Saturday           //do not use colon for a single line
};
int main(){
    enum days day;
    day=Thursday;
    printf("%d",day);
    return 0;
}
3
//the default count is from 0, and you should use colon after the braces to close it

#include<stdio.h>
enum days{
    Monday=1,Tuesday,Wednesday,Thursday,Friday,Saturday         //assign the starting value
};
int main(){
    enum days day;
    day=Thursday;
    printf("%d",day);
    return 0;
}
4