#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(){
    FILE *fp;
    char content[100];
    fp=fopen("sample.txt","r");
    fgets(content,sizeof(content),fp);
    printf("%s",content);
    fclose(fp);
    return 0;
}
Hello to the World of CProgramming!